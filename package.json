{
  "name": "${#project-name#}",
  "version": "1.0.0",
  "description": "${#project-description=none#}",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "repository": "",
  "license": "BSD",
  "${#manifestKey#}": {
    "dependencies": {
      "er": "latest",
      "esui": "latest",
      "ef": "latest"
    },
    "requireConfig": {
      "baseUrl": "src"
    }
  }
}
